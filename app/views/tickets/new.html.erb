<div class="container after-nav">
  <div class="row">
    <div class="col-sm-4 col-sm-offset-4">
      <h2> <%= @event.name %> </h2>
      <ul>
        <% @event.ticket_types.each do |type| %>
          <li> <%= type.name %> </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
